apply plugin: 'com.android.library'


android {
    compileSdkVersion 32
    buildToolsVersion "30.0.2"

    defaultConfig {
        minSdkVersion 21
        targetSdkVersion 32
        versionCode 1
        versionName "1.0"
    }
}


dependencies {
    implementation project(':wanakana-core')
}
